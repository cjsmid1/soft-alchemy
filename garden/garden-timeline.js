const gardenTimeline = [
  {
    date: "2026-01-06",
    type: "photo",
    title: "Garden dreams begin",
    image: "/images/garden/garden-snow-01-2026.jpg",
    markerImage: "/images/handwriting/paw-print-handwritten.png",
    imageSize: "portrait",
    text: "The crafting of a green-fingered vision.",
  },
  {
    date: "2026-01-27",
    type: "photo",
    title: "When in doubt, make a plan",
    image: "/images/garden/garden-plan-layout-01-2026.jpg",
    imageSize: "landscape",
    text: "I was told: this is a small patio kitchen garden with genuine output. It will feel impressive. It will not feel chaotic...<br>If you keep the pot count remotely realistic.",
  },
  {
    date: "2026-01-27",
    type: "photo",
    title: "Break it into tasks",
    image: "/images/garden/garden-plan-tasks-01-2026.jpg",
    imageSize: "square",
    text: "Spoiler: absolutely none of this happens as planned...",
  },
  {
    date: "2026-02-04",
    type: "note",
    title: "First tomato seeds sown",
    text: "Recycling pots collected, knives stabbed into the bottom in the name of drainage, and lovingly misted daily on the sunniest windowsill my house has to offer.",
  },
  {
    date: "2026-02-04",
    type: "note",
    title: "In the name of more garlic!",
    text: "An abandoned terracotta pot discovered in the garden is swiftly repurposed into a garlic kingdom.",
  },
  {
    date: "2026-02-22",
    type: "photo",
    title: "Pot juggling",
    image: "/images/garden/pot-allocation-02-2026.jpg",
    imageSize: "landscape",
    text: "Getting inventive during a no-buy month & raiding my parents' unwanted pots & bucket collection.",
  },
  {
    date: "2026-02-26",
    type: "note",
    title: "Hang fake wasp nest",
    markerImage: "/images/handwriting/bee-icon.png",
    text: "A desperate attempt to avoid a repeat of the Wasp Battle 2025.",
  },
  {
    date: "2026-03-01",
    type: "note",
    title: "Exit no-buy month",
    text: "Immediately splurge on a perfectly reasonable amount of gardening supplies: let the plan commence in earnest!",
  },
  {
    date: "2026-03-03",
    type: "note",
    title: "Seeds pronounced dead ➔ attempt #2",
    markerImage: "/images/handwriting/skull-icon.png",
    text: "Proximity to the radiator and not enough water blamed as the cause... Second seedlings receive thorough soakings and mild elevation courtesy of a tissue box. I get ambitious and plant both Cherry Tomatoes & Mint.",
  },
  {
    date: "2026-03-05",
    type: "note",
    title: "Weather forecast: Rain.",
    text: "I get paranoid about my precious garlic being drowned & move it to the sunnier back part of the garden.",
  },
  {
    date: "2026-03-09",
    type: "note",
    title: "First proper plants arrive!",
    text: "A Patriot Blueberry plant and 5 Albion Strawberry bare roots get lovingly potted into their new homes.",
  },
  {
    date: "2026-03-10",
    type: "note",
    title: "Maybe sowing seeds outdoors will work better...",
    text: "Four ambitious troughs get coated in spinach, chives, lamb's lettuce & spring onion seeds. Tentative daydreams of a harvest by April emerge.",
  },
  {
    date: "2026-03-10",
    type: "photo",
    title: "A couple extra plants can't hurt",
    image: "/images/garden/garden-plan-addition-03-2026.jpg",
    imageSize: "landscape",
    text: "Who can say no to edible flowers?! Also, I can make my own fertiliser? That sounds extremely useful!",
    markerImage: "/images/handwriting/flower-icon.png",
  },
  {
    date: "2026-03-13",
    type: "note",
    title: "",
    text: "Twice as many comfrey bare roots arrive as intended, pot negotiations begin as the comfrey, a daylily, and parsley & coriander seeds all find questionable new homes.",
  },
  {
    date: "2026-03-14",
    type: "note",
    title: "",
    text: "Realise my intel was wrong and I should really get the raspberry plant now rather than wait until April - back to the garden centre!<br>(I choose the Autumn Bliss variety in the hope that means berries sooner rather than an impatient year of waiting.)",
  },
  {
    date: "2026-03-19",
    type: "note",
    title: "",
    text: "Basil & aubergine seeds join the tomato saplings on what is now a very full windowsill.",
  },
  {
    date: "2026-03-23",
    type: "photo",
    title: "The tomatoes get an upgrade",
    image: "/images/garden/window-sill-03-2026.jpeg",
    imageSize: "portrait",
    text: "Finally convinced that the tomatoes shouldn't stay in their tiny seed trays until May and get repotted. Spoiler: only half survived this 'generosity'.",
  },
  {
    date: "2026-03-25",
    type: "photo",
    title: "Getting tactical",
    imageSize: "landscape",
    image: "/images/garden/garden-plan-tactics-03-2026.jpg"
  },
  {
    date: "2026-03-26",
    type: "photo",
    image: "/images/garden/hypertufa-planter-04-2026.jpg",
    imageSize: "square",
    text: "DIY time: creating our own hypertufa planter",
  },
  {
    date: "2026-04-01",
    type: "note",
    title: "Outdoor seeds attempt #2",
    text: "New month, new success? Lamb's lettuce & spring onion seeds both declared in need of a complete resow.",
  },
  {
    date: "2026-04-01",
    type: "note",
    title: "Channel your inner martian",
    text: "I can't wait any longer and plant the 3 potatoes that have been 'chitting' in the kitchen since January. Yes, the grow bag cost more than a full bag of potatoes, I resist spending more money to buy special seed potatoes.",
  },
  {
    date: "2026-04-07",
    type: "photo",
    title: "How concerning is a 40% death rate anyway?",
    image: "/images/garden/strawberry-death-04-2026.jpeg",
    imageSize: "square",
    markerImage: "/images/handwriting/skull-icon.png",
  },
  {
    date: "2026-04-14",
    type: "photo",
    title: "The survivors",
    image: "/images/garden/tomato-death-04-2026.jpeg",
    imageSize: "landscape",
    markerImage: "/images/handwriting/skull-icon.png",
  },
  {
    date: "2026-04-16",
    type: "photo",
    title: "Gentle progress",
    image: "/images/garden/garden-saplings-04-2026.jpeg",
    imageSize: "portrait",
    text: "Presenting the first green growth of the potatoes, what I believed to be the future of my salad collection & a fresh pot of leek seeds sown in a clever gully technique that backfired instantly."
  },

  {
    date: "2026-05-06",
    type: "photo",
    image: "/images/garden/label-mistake-05-2026.jpg",
    imageSize: "square",
    text: "Lesson learned the hard way: label all your pots.<br>Time to sow some actual mint seeds..."
  },
  {
    date: "2026-05-07",
    type: "photo",
    title: "Google, please identify these plants...",
    image: "/images/garden/weed-trough-05-2026.jpeg",
    imageSize: "portrait",
    text: "Being gaslit into thinking these assorted weeds are my beloved lamb's lettuce."
  },
  {
    date: "2026-05-07",
    type: "photo",
    image: "/images/garden/comfrey-05-2026.jpg",
    imageSize: "portrait",
    text: "The time has come for you to be made into fertiliser!"
  },
  {
    date: "2026-05-08",
    type: "photo",
    title: "Try, try again",
    image: "/images/garden/bernie-sanders-meme.jpg",
    imageSize: "square",
    text: "Attempt #RapidlyLosingHope: the chives, spinach & spring onions have been resown once more.",
  },
  {
    date: "2026-05-09",
    type: "photo",
    title: "Getting technical",
    image: "/images/garden/sunpath-05-2026.png",
    imageSize: "square",
    text: "What actually is the sunniest part of the garden anyway?"
  },
  {
    date: "2026-05-13",
    type: "photo",
    title: "Slow & steady?",
    image: "/images/garden/tiny-aubergine-05-2026.jpg",
    imageSize: "landscape",
    text: "Why yes, these are the basil and aubergine seeds I planted two months ago. Yes, of course they should still be this small!"
  },
  {
    date: "2026-05-14",
    type: "note",
    text: "Heat mat bought in an attempt to throw money at the basil problem.",
  },
  {
    date: "2026-05-15",
    type: "note",
    text: "Most tomato saplings lovingly moved out, and the dawn of the cobra climbing bean era.",
  },
  {
    date: "2026-05-20",
    type: "photo",
    title: "Bird Alert!",
    image: "/images/garden/bird-alert-05-2026.jpg",
    imageSize: "portrait",
    markerImage: "/images/handwriting/paw-print-handwritten.png",
    text: "The first recorded use of the windowsill bird feeder by the blue tits! Yes, this is the best photo I managed to get, I was too busy being excited!"
  },
  {
    date: "2026-05-21",
    type: "note",
    title: "Heat wave incoming...",
    text: "Mulch everything! Final tomato plants outside now! Woodchip topping and buried ollas on standby! Move, people!",
  },
  {
    date: "2026-05-25",
    type: "photo",
    image: "/images/garden/sad-leek-05-2026.jpeg",
    imageSize: "portrait",
    text: "Worthy warriors: you have survived countless soil cave-ins and emergency excavations. You are the chosen ones."
  },
  {
    date: "2026-05-26",
    type: "photo",
    title: "Small rewards",
    image: "/images/garden/daylily-flower-05-2026.jpg",
    imageSize: "square",
    markerImage: "/images/handwriting/flower-icon.png",
    text: "It's so pretty, and it's edible too... but I don't want to pick it. Argh!"
  },
  {
    date: "2026-05-27",
    type: "note",
    title: "Feeding time",
    text: "Will the comfrey fertiliser 'tea' make any difference? Who knows!",
  },
  {
    date: "2026-05-27",
    type: "note",
    text: "Lesson learned the hard way: harden plants gently. Turns out when basil says it likes sun, it doesn't mean 6 hours over midday during a heatwave. Six living saplings went out, three lightly cremated saplings returned. Death rate 50%.",
    markerImage: "/images/handwriting/skull-icon.png",
  },
];

function renderGardenTimeline(containerId) {
  const container = document.getElementById(containerId);
  if (!container || typeof gardenTimeline === "undefined") return;

  const monthOrder = ["2026-01", "2026-02", "2026-03", "2026-04", "2026-05"];

  const sortedItems = [...gardenTimeline].sort(
    (a, b) => new Date(a.date) - new Date(b.date)
  );

  const groupedItems = monthOrder.map((monthKey) => ({
    monthKey,
    label: formatGardenMonth(monthKey),
    items: sortedItems.filter((item) => item.date.startsWith(monthKey)),
  }));

  container.innerHTML = groupedItems
    .map((month) => {
      const columns = balanceTimelineItems(month.items);

      return `
  <section class="timeline-month-section" id="timeline-${month.monthKey}">
    <div class="timeline-month-marker">${month.label}</div>

    <div class="timeline-month-items">
      <svg class="timeline-thread-layer" aria-hidden="true"></svg>

      <div class="timeline-marker-layer">
        ${month.items.map(renderTimelineMarker).join("")}
      </div>

      <div class="timeline-column timeline-column-left">
        ${columns.left.map(renderTimelineEntry).join("")}
      </div>

      <div class="timeline-column timeline-column-right">
        ${columns.right.map(renderTimelineEntry).join("")}
      </div>
    </div>
  </section>
`;
    })
    .join("");

  setupTimelineMarkerHover(container);
  setupTimelineEntryHover(container);
  setupTimelineImageLightbox(container);
  renderTimelineMonthJump(groupedItems);
}

function isMobileTimeline() {
  return window.matchMedia("(max-width: 700px)").matches;
}

function getTimelineTilt(seed) {
  let hash = 0;

  for (let i = 0; i < seed.length; i++) {
    hash = seed.charCodeAt(i) + ((hash << 5) - hash);
  }

  const tilts = [-1.4, 1.1, -0.8, 1.5, -1.1, 0.9];
  return tilts[Math.abs(hash) % tilts.length];
}

function renderTimelineEntry(item) {
  const entryId = getTimelineEntryId(item);
  const dateGroup = getTimelineDateGroup(item);
  const spacer = item.timelineSpacer || 0;
  const tilt = getTimelineTilt(entryId);

  return `
    <article 
      class="timeline-entry timeline-${item.type}" 
      data-entry="${entryId}"
      data-date-group="${dateGroup}"
      style="margin-top: ${spacer}rem; --timeline-tilt: ${tilt}deg;"
    >
      ${renderTimelineItem(item)}
    </article>
  `;
}

function getDayPositionInMonth(dateString) {
  const date = new Date(dateString);
  const year = date.getFullYear();
  const month = date.getMonth();
  const day = date.getDate();

  const daysInMonth = new Date(year, month + 1, 0).getDate();

  return ((day - 1) / (daysInMonth - 1)) * 100;
}

function renderTimelineMarker(item) {
  const dateGroup = getTimelineDateGroup(item);
  const position = getDayPositionInMonth(item.date);

  const markerContent = item.markerImage
    ? `<img src="${item.markerImage}" alt="" class="timeline-marker-image">`
    : `<span class="timeline-marker-dot"></span>`;

  return `
    <button 
      class="timeline-marker" 
      type="button" 
      data-date-group="${dateGroup}" 
      style="top: ${position}%"
      aria-label="Highlight entries for ${formatGardenDate(item.date)}"
    >
      ${markerContent}
    </button>
  `;
}

function renderTimelineItem(item) {
  const date = formatGardenDate(item.date);

  if (item.type === "photo") {
    const imageSrc = item.image;

    return `
      <div class="timeline-date">${date}</div>
      ${item.title ? `<h3>${item.title}</h3>` : ""}
      ${imageSrc
        ? `<img class="timeline-image" src="${imageSrc}" alt="${item.title || "Garden timeline photo"}">`
        : ""
      }
      ${item.text ? `<p>${item.text}</p>` : ""}
    `;
  }

  if (item.type === "post") {
    return `
      <div class="timeline-date">${date}</div>
      ${item.title ? `<h3>${item.title}</h3>` : ""}
      <div id="timeline-post-${item.postId}"></div>
    `;
  }

  return `
    <div class="timeline-date">${date}</div>
    ${item.title ? `<h3>${item.title}</h3>` : ""}
    ${item.text ? `<p>${item.text}</p>` : ""}
  `;
}

function balanceTimelineItems(items) {
  const columns = { left: [], right: [] };

  const MAX_SPACER = 2; // prevents giant empty holes
  const MAX_DISTANCE_FROM_MARKER = 3;

  const estimatedTotalHeight = items.reduce(
    (sum, item) => sum + estimateTimelineItemWeight(item),
    0
  );

  // Month height now adapts to actual amount of content
  const monthHeight = Math.max(14, estimatedTotalHeight / 2 + items.length * 1.5);

  let leftCursor = 0;
  let rightCursor = 0;

  items.forEach((item, index) => {
    const itemHeight = estimateTimelineItemWeight(item);
    const markerPosition = getDayPositionInMonth(item.date) / 100;
    const markerY = markerPosition * monthHeight;

    let chosenSide = index === 0
      ? "left"
      : leftCursor <= rightCursor
        ? "left"
        : "right";

    let cursor = chosenSide === "left" ? leftCursor : rightCursor;

    let spacer = Math.max(0, markerY - cursor - MAX_DISTANCE_FROM_MARKER);

    // Important: cap the spacer so sparse months don't become enormous
    spacer = Math.min(spacer, MAX_SPACER);

    const layoutItem = {
      ...item,
      timelineSpacer: spacer,
    };

    columns[chosenSide].push(layoutItem);

    const newCursor = cursor + spacer + itemHeight;

    if (chosenSide === "left") {
      leftCursor = newCursor;
    } else {
      rightCursor = newCursor;
    }
  });

  return columns;
}

function estimateTimelineItemWeight(item) {
  let height = 4;

  if (item.title) height += 1.2;
  if (item.text) height += item.text.length / 80;

  if (item.image) {
    switch (item.imageSize) {
      case "landscape":
        height += 3;
        break;

      case "square":
        height += 7;
        break;

      case "portrait":
        height += 9;
        break;

      default:
        height += 8;
    }
  }

  return height;
}

function setupTimelineMarkerHover(container) {
  container.querySelectorAll(".timeline-marker").forEach((marker) => {
    marker.addEventListener("mouseenter", () => {
      activateTimelineThread(container, marker);
    });

    marker.addEventListener("mouseleave", () => {
      clearTimelineThreads(container);
    });

    marker.addEventListener("focus", () => {
      activateTimelineThread(container, marker);
    });

    marker.addEventListener("blur", () => {
      clearTimelineThreads(container);
    });
  });
}

function activateTimelineThread(container, marker) {
  if (isMobileTimeline()) return;
  const dateGroup = marker.dataset.dateGroup;

  clearTimelineThreads(container);

  marker.classList.add("timeline-marker-active");

  const monthItems = marker.closest(".timeline-month-items");
  const svg = monthItems.querySelector(".timeline-thread-layer");

  const entries = monthItems.querySelectorAll(
    `.timeline-entry[data-date-group="${dateGroup}"]`
  );

  entries.forEach((entry) => {
    entry.classList.add("timeline-highlight");
    drawTimelineThread(monthItems, svg, marker, entry);
  });
}

function setupTimelineEntryHover(container) {
  container.querySelectorAll(".timeline-entry").forEach((entry) => {
    entry.addEventListener("mouseenter", () => {
      activateTimelineThreadFromEntry(container, entry);
    });

    entry.addEventListener("mouseleave", () => {
      clearTimelineThreads(container);
    });
  });
}

function activateTimelineThreadFromEntry(container, entry) {
  if (isMobileTimeline()) return;
  const dateGroup = entry.dataset.dateGroup;

  clearTimelineThreads(container);

  entry.classList.add("timeline-highlight");

  const monthItems = entry.closest(".timeline-month-items");
  const svg = monthItems.querySelector(".timeline-thread-layer");

  const marker = monthItems.querySelector(
    `.timeline-marker[data-date-group="${dateGroup}"]`
  );

  if (!marker) return;

  marker.classList.add("timeline-marker-active");

  drawTimelineThread(monthItems, svg, marker, entry, true);
}

function clearTimelineThreads(container) {
  container.querySelectorAll(".timeline-highlight").forEach((entry) => {
    entry.classList.remove("timeline-highlight");
  });

  container.querySelectorAll(".timeline-marker-active").forEach((marker) => {
    marker.classList.remove("timeline-marker-active");
  });

  container.querySelectorAll(".timeline-thread-layer").forEach((svg) => {
    svg.innerHTML = "";
  });
}

function drawTimelineThread(wrapper, svg, marker, entry, reverse = false) {
  const wrapperRect = wrapper.getBoundingClientRect();
  const markerRect = marker.getBoundingClientRect();
  const entryRect = entry.getBoundingClientRect();

  const markerX = markerRect.left + markerRect.width / 2 - wrapperRect.left;
  const markerY = markerRect.top + markerRect.height / 2 - wrapperRect.top;

  const entryX =
    entryRect.left < markerRect.left
      ? entryRect.right - wrapperRect.left
      : entryRect.left - wrapperRect.left;

  const entryY = entryRect.top + entryRect.height / 2 - wrapperRect.top;

  const controlX = (markerX + entryX) / 2;

  const path = document.createElementNS("http://www.w3.org/2000/svg", "path");

  path.setAttribute(
    "d",
    `M ${markerX} ${markerY} C ${controlX} ${markerY}, ${controlX} ${entryY}, ${entryX} ${entryY}`
  );

  path.setAttribute("class", "timeline-thread-path");

  svg.appendChild(path);

  const length = path.getTotalLength();

  path.style.strokeDasharray = length;
  path.style.strokeDashoffset = reverse ? -length : length;

  // Force browser to apply the hidden state first
  path.getBoundingClientRect();

  requestAnimationFrame(() => {
    requestAnimationFrame(() => {
      path.classList.add("timeline-thread-draw");
      path.style.strokeDashoffset = 0;
    });
  });
}

function getTimelineEntryId(item) {
  return `${item.date}-${(item.title || item.text || item.type)
    .toLowerCase()
    .replace(/<br>/g, "-")
    .replace(/[^a-z0-9]+/g, "-")
    .replace(/^-|-$/g, "")}`;
}

function getTimelineDateGroup(item) {
  return item.date;
}

function formatGardenDate(dateString) {
  return new Date(dateString).toLocaleDateString("en-GB", {
    day: "numeric",
    month: "short",
  });
}

function formatGardenMonth(monthKey) {
  const [year, month] = monthKey.split("-");

  return new Date(Number(year), Number(month) - 1).toLocaleDateString("en-GB", {
    month: "short",
  });
}

function setupTimelineImageLightbox(container) {
  container.querySelectorAll(".timeline-image").forEach((image) => {
    image.addEventListener("click", () => {
      openTimelineLightbox(image.src, image.alt);
    });
  });
}

function openTimelineLightbox(src, alt = "") {
  const lightbox = document.createElement("div");
  lightbox.className = "timeline-lightbox";
  lightbox.innerHTML = `
  <div class="timeline-lightbox-content">
    <button class="timeline-lightbox-close" type="button" aria-label="Close image">×</button>
    <img src="${src}" alt="${alt}" class="timeline-lightbox-image">
  </div>
`;

  document.body.appendChild(lightbox);
  document.body.classList.add("lightbox-open");

  function closeLightbox() {
    lightbox.remove();
    document.body.classList.remove("lightbox-open");
    document.removeEventListener("keydown", handleKeydown);
  }

  function handleKeydown(event) {
    if (event.key === "Escape") closeLightbox();
  }

  lightbox.addEventListener("click", (event) => {
    if (
      event.target === lightbox ||
      event.target.classList.contains("timeline-lightbox-close")
    ) {
      closeLightbox();
    }
  });

  document.addEventListener("keydown", handleKeydown);
}

function renderTimelineMonthJump(groupedItems) {
  const jumpList = document.getElementById("timelineMonthJump");
  if (!jumpList) return;

  jumpList.innerHTML = groupedItems
    .filter((month) => month.items.length > 0)
    .map(
      (month) => `
        <li>
          <a href="#timeline-${month.monthKey}">${month.label}</a>
        </li>
      `
    )
    .join("");

  jumpList.querySelectorAll("[data-month-target]").forEach((button) => {
    button.addEventListener("click", () => {
      const target = document.getElementById(button.dataset.monthTarget);
      if (!target) return;

      target.scrollIntoView({
        behavior: "smooth",
        block: "start",
      });
    });
  });
}